{% extends 'base.html.twig' %}
{%  block main %}
    <article>
        <h1>{{ article.title }}</h1>
            <div>{{ article.content }}</div>
            <p>{{ article.createdAt | date('y-m-d') }}</p>
        </h1>
    </article>
{%  endblock main %}